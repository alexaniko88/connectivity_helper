name: connectivity_helper
description: "A flutter package which helps with the app connectivity management"
repository: https://github.com/alexaniko88/connectivity_helper.git

version: 0.0.1

environment:
  sdk: '>=3.2.6 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  connectivity_plus: ^5.0.2
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter: